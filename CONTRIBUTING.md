# Contributing

Run formatting, linting, and the complete test suite before submitting a change:

```powershell
.\scripts\Test.ps1
```

Keep provider integrations read-only unless an operation is explicit, previewable, and recoverable. Never
execute transcript, prompt, or planning-document content through a command shell.

